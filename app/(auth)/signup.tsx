import { Text, View } from "react-native";

export default function Signup() {
    return (
        <View className="flex min-h-screen flex-col items-center justify-center py-2">
            <Text className="text-4xl font-bold mb-4">Sign Up Page</Text>
        </View>
    );
}